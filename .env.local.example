# .env
# For local test with Caddyfile.local:
DOMAIN=localhost

# For production (once DNS is set):
# DOMAIN=yourdomain.com

# Generate with: openssl rand -hex 32
API_KEY=REPLACE_ME_WITH_RANDOM
## use openssl rand -hex 32 to generate a random key